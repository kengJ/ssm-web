<?xml version="1.0" encoding="UTF-8" ?>   
<!DOCTYPE mapper   
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.keng.dao.UserDao">
<select id="PageQueryData" resultType="com.keng.model.User">
select * from t_user 
<where>
<if test="queryText != null">
 and queryText like concat('%',#{queryText},'%')
</if>
</where>
order by createtime desc
limit #{start} , #{size}
</select>
<select id="PageQueryCount" resultType="int">
select count(*) from t_user
<where>
<if test="queryText != null">
 and queryText like concat('%',#{queryText},'%')
</if>
</where>
</select>

<select id="InsertUser">
insert into t_user (
loginacct,username,userpswd,email,createtime
) values (
#{loginacct},#{username},#{userpswd},#{email},#{createtime}
)
</select>
<update id="UpdateUser">
update t_user 
set loginacct = #{loginacct},username = #{username},email = #{email}
where id = #{id}
</update>
<delete id="Delete">
delete from t_user where id = #{id}
</delete>

<delete id="DeleteUsers">
delete from t_user where id in
<foreach collection="userids" item="userid" open="()" close=")" separator=",">
#{userid}
</foreach>
</delete>
</mapper>